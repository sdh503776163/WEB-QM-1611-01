js是基于对象和事件驱动的脚本语言
主要运行于客户端（浏览器）
单线程
无需预编译

js组成
	ECMAscript  简称ES
	DOM			文档对象模型
	BOM			浏览器对象模型

ES:
	语法
	类型
	语句
	关键字
	保留字
	操作符
	对象

DOM:
	将整个页面映射为一个多层节点结构（DOM树）
	可以借助DOM提供的API(接口)针对节点进行删除、添加、替换、修改

BOM：
	可以控制浏览器显示页面以外的部分

